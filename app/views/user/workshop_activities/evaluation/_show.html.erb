<%= content_for :menu_itens do %>
  <li>
    <%= link_to user_workshop_path(@workshop) do %>
      <%= t('.back_workshop') %>
      <span class="icon20 back"></span>
    <% end %>
  </li>
<% end %>
<%= content_for :left_col_container do %>
	<%= content_tag(:div, class:"panel flat user-activity-responses") do %>
	  <div class="panel-content">
	    <%= render 'user/workshop_activities/evaluation/students' %>
	  </div>
	<% end %> 	
<% end %>

<div id="activity-responses">
	<%= render partial:'user/workshop_activities/evaluation/responses', 
							locals: {activity_response:@activity_responses.first} %>
</div>

