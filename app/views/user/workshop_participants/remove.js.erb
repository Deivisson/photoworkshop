<% unless @workshop_participant.errors.any? %>
	$('#participant-<%= "#{@workshop.id}#{@workshop_participant.user_id}" %>').remove();
	$("#user-" + '<%= @workshop_participant.user_id %>').remove();
	flashNoticeMessage("<%= t('messages.user_removed_from_workshop') %>");
<% end %>