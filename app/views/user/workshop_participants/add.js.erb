<% if @workshop_participant.errors.any? %>
	
<% else %>
	$("#workshop-participants-container").append("<%= escape_javascript(render partial:'participant',locals:{participant:@participant})%>")
	$('#user-<%= "#{@workshop_participant.user_id}" %>').remove();
<% end %>