<%= semantic_form_for [:user,@photo,PhotoComment.new], remote:true do |f| %>
  <%= f.inputs do %>
    <%= f.input :content,
  							:input_html => {rows:3},
  							:label => false %>
  <% end %>
  <%= submit_tag t('buttons.comment'), class:'submit-button' %>
<% end %>
