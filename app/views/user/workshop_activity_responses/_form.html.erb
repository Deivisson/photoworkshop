<%= semantic_form_for [:user,@workshop_activity_response] do |f| %>
  <%= f.semantic_fields_for :photos do |photo| %>
    <%= render partial:'user/photos/fields', locals: {f: photo} %>
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit, :as => :input %>
  <% end %>
<% end %>
