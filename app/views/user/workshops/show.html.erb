<% if @workshop.mine?(current_user) %>
	<%= render 'user/workshops/show_owner/show' %>
	<%# render 'user/workshops/show_student/show' %>
<% elsif @workshop.enrolled?(current_user) %>
	<%= render 'user/workshops/show_student/show' %>
<% end %>