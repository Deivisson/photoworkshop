//$('#home-timeline').append('<%= j render 'timeline' %>');
$('<%= j render 'timeline' %>').insertBefore( $( ".pagination" ) );
<% if @photos.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @photos %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
TimeLineListEffect();
photoView();	
PhotoPopupMenuTrigger();
photographerPreview();
PhotoAvoidRightClick();