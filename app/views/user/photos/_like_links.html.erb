<% if @photo.admirer_ids.include?(current_user.id) %> 
  <%= link_to "Unlike",user_photo_unlike_path(@photo.id),remote:true %> 
<% else %>
  <%= link_to "Like",user_photo_like_path(@photo.id),remote:true %> 
<% end %>