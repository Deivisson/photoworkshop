<% facebook_image_share @photo_cover.picture.url(:large), t('home.welcome.texts.sub_header') %>
<%= content_for :login_form do %>
	<%= form_for(:user, :url => session_path(:user), html:{class:"login-form"}) do |f| %>
	  <%= f.email_field :email, 
	  									autofocus: true, 
	  									class:"username",
	  									placeholder: "Email ou usuário" %>

    <%= f.password_field :password, 
    											autocomplete: "off", 
    											class:"password",
    											placeholder: "Senha" %>
    <div class="remember-me">
  		<%= f.check_box :remember_me %> 
  		<%= f.label :remember_me %>
  	</div>
	<% end %>
<% end %>